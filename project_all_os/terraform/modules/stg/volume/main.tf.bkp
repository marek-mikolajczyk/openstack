/*
resource "openstack_blockstorage_volume_v2" "volume" {
  name = var.volume_name
  size = var.volume_size
}

resource "openstack_compute_volume_attach_v2" "volume_attached" {
  instance_id = var.instance_id
  volume_id   = var.volume_id
}
*/